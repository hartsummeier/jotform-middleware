{
  "intake_form_id": "252318171421044",
  "include_all_questions": true,

  // Optional: skip non-input controls if you want to be explicit.
  "exclude_types": [
    "control_head",
    "control_pagebreak",
    "control_button",
    "control_image",
    "control_text",
    "control_divider",
    "control_collapse",
    "control_signature",
    "control_matrix"
  ],

  // Optional: if you later need to force a specific type for a field
  // or rename a key, put it here by Unique Name.
  "overrides": {
    // "PurchasePrice": { "type": "number" },
    // "ClosingDate":   { "type": "date" }
  },

  // Leave true unless you run into unexpected model refusals.
  "strict_schema": true
}
